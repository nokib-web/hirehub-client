export const metadata = {
  title: "Contact Us | HireHub",
  description: "Get in touch with the HireHub team for support, partnerships, or general inquiries.",
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
