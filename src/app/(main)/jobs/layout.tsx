export const metadata = {
  title: "Browse Jobs | HireHub",
  description: "Find your dream job across top companies actively hiring.",
};

export default function JobsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
